import { FormControlBaseDirective } from './form-control-base.directive';

describe('Directive: FormControlBase', () => {
  it('should create an instance', () => {
    const directive = new FormControlBaseDirective();
    expect(directive).toBeTruthy();
  });
});
