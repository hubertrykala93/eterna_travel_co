import { TransformUrlPipe } from './transform-url.pipe';

describe('Pipe: TransformUrle', () => {
  it('create an instance', () => {
    const pipe = new TransformUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
